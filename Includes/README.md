This directory is where external build artifacts should be copied.

One dependency is required to build the custom device:
- `DSF Core.lvlibp` from niveristand-data-sharing-framework-custom-device\Source\Core
